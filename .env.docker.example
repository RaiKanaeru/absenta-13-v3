# ========================================
# ABSENTA 13 - Docker Environment Variables
# ========================================
# Copy this file to .env and fill in the values
# ========================================

# ==========================================
# DATABASE - MySQL Configuration
# ==========================================
DB_HOST=mysql
DB_PORT=3306
DB_NAME=absenta13
DB_USER=absenta
DB_PASSWORD=your_secure_db_password_here

# MySQL root password (required for initial setup)
MYSQL_ROOT_PASSWORD=your_mysql_root_password_here

# ==========================================
# CACHE - Redis Configuration  
# ==========================================
REDIS_HOST=redis
REDIS_PORT=6379

# ==========================================
# SECURITY - JWT & Encryption
# ==========================================
# Generate with: openssl rand -base64 64
JWT_SECRET=your_super_secret_jwt_key_here_at_least_64_characters_long

# Password hashing rounds (higher = more secure but slower)
SALT_ROUNDS=12

# ==========================================
# APPLICATION Settings
# ==========================================
NODE_ENV=production
PORT=3001

# Allowed origins for CORS (comma separated)
ALLOWED_ORIGINS=https://absenta13.my.id,https://www.absenta13.my.id

# Upload directory (inside container)
UPLOAD_DIR=/app/public/uploads

# ==========================================
# OPTIONAL - Email Configuration
# ==========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# ==========================================
# OPTIONAL - Backup Configuration
# ==========================================
# BACKUP_RETENTION_DAYS=30
# BACKUP_SCHEDULE=0 2 * * *
