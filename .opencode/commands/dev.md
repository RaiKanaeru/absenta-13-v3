---
description: Start local full-stack development mode
---

Run `npm run dev:full`.

If startup fails:
- Report which process failed first (frontend or backend).
- Include the first actionable error stack.
- Suggest one minimal fix at a time.
