---
description: Create a new React component with TypeScript
---

Create a new React component named `$ARGUMENTS` in `src/components/`.

Requirements:
- Use TypeScript with proper typing for props
- Use functional component pattern
- Follow project conventions from AGENTS.md
- Use Tailwind CSS for styling
- Import from `@/` alias paths
- Include basic JSDoc comments
